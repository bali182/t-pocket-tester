export const getComponentNodeId = (componentId: string): string => `component:${componentId}`

export const getHoleNodeId = (holeId: string): string => `hole:${holeId}`

export const getStitchLineNodeId = (stitchLineId: string): string => `stitch-line:${stitchLineId}`
